git init

git add .

git config --global user.email "benrmcdavid@gmail.com"

git config --global user.name "Ben McDavid"

git commit -m "First commit"

git remote add origin "https://github.com/brmcdavid/mcdavidtraining"

git remote -v

git pull origin master

git push origin master
